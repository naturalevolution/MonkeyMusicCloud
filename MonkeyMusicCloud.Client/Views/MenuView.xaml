<UserControl x:Class="MonkeyMusicCloud.Client.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:MonkeyMusicCloud.Client.ViewModels" mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="800">
    <UserControl.DataContext >
        <local:MenuViewModel /> 
    </UserControl.DataContext>
    
    <Menu ItemsSource="{Binding BodyViews}" >
        
        <Menu.ItemTemplate>
            <DataTemplate>
                <Button Command="{Binding ChangeViewCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter />
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="{Binding MenuLabel}"/>
                </Button>
            </DataTemplate>
        </Menu.ItemTemplate>
    </Menu>
</UserControl>