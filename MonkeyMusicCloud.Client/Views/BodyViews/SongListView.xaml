<UserControl x:Class="MonkeyMusicCloud.Client.Views.BodyViews.SongListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:BodyViews="clr-namespace:MonkeyMusicCloud.Client.Views.BodyViews" 
             xmlns:BodyViewModels="clr-namespace:MonkeyMusicCloud.Client.ViewModels.BodyViewModels"
             mc:Ignorable="d" d:DesignWidth="800">
    <UserControl.DataContext>
        <BodyViewModels:SongListViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="700" />
        </Grid.ColumnDefinitions>
        <Button Content="Play All" /> <!-- A Implementer -->
        <ListView HorizontalAlignment="Stretch" Grid.Row="1" Grid.ColumnSpan="2"
          Name="lvSongList" VerticalAlignment="Top" Width="800" Margin="0,0,0,0" ItemsSource="{Binding SongList, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView AllowsColumnReorder="true"  ColumnHeaderToolTip="Musics">
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Path=Title}"  />
                    <GridViewColumn Header="Album">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" >
                                    <Button Command="{Binding  DataContext.OpenAlbumCommand,
                                                                                  RelativeSource={RelativeSource FindAncestor,
                                                                                                                 AncestorType={x:Type BodyViews:SongListView}}}"                                        
                                                                       CommandParameter="{Binding Path=Album}">
                                        <Button.Template>
                                            <ControlTemplate>
                                                <Label Content="{Binding Path=Album}" />
                                            </ControlTemplate>
                                        </Button.Template>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Cursor" Value="Hand" />
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Artist" DisplayMemberBinding="{Binding Path=Artist}" />
                    <GridViewColumn>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="24" Height="24" Command="{Binding  DataContext.AddSongToPlayListCommand,
                                                                                  RelativeSource={RelativeSource FindAncestor,
                                                                                                                 AncestorType={x:Type BodyViews:SongListView}}}"  
                                                               CommandParameter="{Binding}">
                                    <Image Source="../../Themes/Images/add.png" />
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                        
                        <!-- <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Width="24" Height="24" Command="{Binding  DataContext.AddSongToPlayListCommand,
                                                                                  RelativeSource={RelativeSource FindAncestor,
                                                                                                                 AncestorType={x:Type BodyViews:SongListView}}}"  
                                                               CommandParameter="{Binding ElementName=lvSongList, Path=SelectedItems}">
                                    <Image Source="../../Themes/Images/add.png" />
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                        -->
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
