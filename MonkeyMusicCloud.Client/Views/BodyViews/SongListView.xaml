<UserControl x:Class="MonkeyMusicCloud.Client.Views.BodyViews.SongListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:MonkeyMusicCloud.Client.ViewModels" xmlns:BodyViews="clr-namespace:MonkeyMusicCloud.Client.Views.BodyViews" mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModels:SongListViewModel />
    </UserControl.DataContext>
    <Grid>
        <ListView HorizontalAlignment="Left" Name="lvSongList" VerticalAlignment="Top" Height="600" Width="800" Margin="0,0,0,0" ItemsSource="{Binding SongList, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}">
            <ListView.View>
                <GridView AllowsColumnReorder="true"  ColumnHeaderToolTip="Musics">
                    <GridViewColumn>
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <Button Width="24" Height="24"
                                        Command="{Binding DataContext.RefreshSongListCommand,
                                                          RelativeSource={RelativeSource FindAncestor,
                                                                                         AncestorType={x:Type BodyViews:SongListView}}}">
                                    <Image Source="../../Themes/Images/refresh.png" />
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>                                
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Titre" DisplayMemberBinding="{Binding Path=Length}" />
                    <GridViewColumn>
                        <GridViewColumn.HeaderTemplate>
                            <DataTemplate>
                                <Button Width="24" Height="24" Command="{Binding  DataContext.AddSongToPlayListCommand,
                                                                                  RelativeSource={RelativeSource FindAncestor,
                                                                                                                 AncestorType={x:Type BodyViews:SongListView}}}"  
                                                               CommandParameter="{Binding ElementName=lvSongList, Path=SelectedItems}">
                                    <Image Source="../../Themes/Images/add.png" />
                                </Button>
                            </DataTemplate>
                        </GridViewColumn.HeaderTemplate>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
