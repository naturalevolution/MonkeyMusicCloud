<UserControl x:Class="MonkeyMusicCloud.Client.Views.BodyViews.SongDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:ViewModels="clr-namespace:MonkeyMusicCloud.Client.ViewModels" mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
<!--TODO : Design à faire -->
    <Grid>
        <Grid.DataContext>
            <ViewModels:AddSongsViewModel />
        </Grid.DataContext>
        <TextBox Text="{Binding SongPath, Mode=TwoWay}"/>
        <Button Command="{Binding AddSongCommand}" Content="Add musics"></Button>
    </Grid>
</UserControl>
